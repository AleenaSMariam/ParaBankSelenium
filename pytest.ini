[pytest]
addopts = 
    -v
    --strict-markers
    --tb=short
    --timeout=30
    
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: Smoke tests
    regression: Regression tests
    registration: Registration tests
    billpay: Bill Pay tests